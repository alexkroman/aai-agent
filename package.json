{
  "name": "aai-agent",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix platform",
    "start": "npm start --prefix platform",
    "build": "npm run build --prefix platform",
    "test": "npm test --prefix platform",
    "typecheck": "npm exec --prefix platform -- tsc --noEmit"
  }
}
